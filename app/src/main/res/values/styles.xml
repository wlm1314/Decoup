<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="autoCompleteTextViewStyle">@style/AppAutoCompleteTextView</item>
    </style>

    <style name="searchView" parent="Widget.AppCompat.SearchView">
        <item name="defaultQueryHint">单号/企业名称</item>
        <item name="searchIcon">@mipmap/ic_search</item>
        <item name="searchHintIcon">@mipmap/ic_search</item>
    </style>

    <!--修改searchView字体颜色、大小-->
    <style name="AppAutoCompleteTextView" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:textColor">@color/text_normal</item>
        <item name="android:textAppearance">@style/AutoCompleteTextAppearance</item>
    </style>

    <style name="AutoCompleteTextAppearance" parent="android:TextAppearance">
        <item name="android:textColor">@color/text_normal</item>
        <item name="android:textColorHint">@color/text_hint</item>
        <item name="android:textColorHighlight">@color/text_hint</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="radio_button_line">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="AnimDown" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_scale_in</item>
        <item name="android:windowExitAnimation">@anim/push_scale_out</item>
    </style>

</resources>
